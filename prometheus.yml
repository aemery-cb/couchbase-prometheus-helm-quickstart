prometheus:
  prometheusSpec:
    additionalServiceMonitors:
      - name: couchbase
        additionalLabels:
          - app: couchbase
        endpoints:
          - port: metrics
            interval: 5s
        namespaceSelector:
          matchNames:
            - default
        selector:
          matchLabels:
            app: couchbase
